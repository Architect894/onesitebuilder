"use client";

/**
 * Template: Conversion + Bold Modern
 * 
 * Layout: Hero → Services → Features → Testimonials → CTA
 * Style: Vibrant modern tech aesthetic
 */

import { createTemplate } from "@/templates/factories/createTemplate";
import { SKELETON_CONVERSION } from "@/templates/skeletons/definitions";
import { STYLE_BOLD_MODERN } from "@/templates/styles/presets";

export default createTemplate(SKELETON_CONVERSION, STYLE_BOLD_MODERN);
